import { StrConvertorPipe } from './str-convertor.pipe';

describe('StrConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new StrConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
