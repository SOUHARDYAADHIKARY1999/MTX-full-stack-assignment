import { StrSlicerPipe } from './str-slicer.pipe';

describe('StrSlicerPipe', () => {
  it('create an instance', () => {
    const pipe = new StrSlicerPipe();
    expect(pipe).toBeTruthy();
  });
});
